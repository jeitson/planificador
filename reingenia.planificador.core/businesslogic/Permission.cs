using System.Collections.Generic;

//using reingenia.planificador.data.access;
//using reingenia.planificador.entity;
//using reingenia.planificador.@interface;

namespace reingenia.planificador.BusinessLogic
{
    /*
    public class Permission : IPermission
    {

        internal PermissionDao permissionDao = null;

        internal PermissionDao PermissionDao
        {
            get
            {
                if (permissionDao == null)
                    permissionDao = new PermissionDao();
                
                return permissionDao;
            }
        }

        public PermissionInfo Create(PermissionInfo item)
        {
            return PermissionDao.Create(item);
        }

        public PermissionInfo Update(PermissionInfo item)
        {
            return PermissionDao.Update(item);
        }

        public bool Delete(int id)
        {
            PermissionDao.Delete(new PermissionInfo() { Id = id });

            return true;
        }

        public PermissionInfo Get(int id)
        {
            return PermissionDao.Find(id);
        }

        public List<PermissionInfo> List()
        {
            return PermissionDao.List();
        }

        public List<PermissionInfo> GetByUser(int userId)
        {
            return PermissionDao.GetByUser(userId);
        }

    }
    */
}
