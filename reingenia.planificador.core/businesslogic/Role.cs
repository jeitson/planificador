using System.Collections.Generic;

//using reingenia.planificador.data.access;
//using reingenia.planificador.entity;
//using reingenia.planificador.@interface;

namespace reingenia.planificador.BusinessLogic
{
    /*
    public class Role : IRole
    {

        internal RoleDao roleDao = null;

        internal RoleDao RoleDao
        {
            get
            {
                if (roleDao == null)
                    roleDao = new RoleDao();

                return roleDao;
            }
        }

        public RoleInfo Create(RoleInfo item)
        {
            return RoleDao.Create(item);
        }

        public RoleInfo Update(RoleInfo item)
        {
            return RoleDao.Update(item);
        }

        public bool Delete(int id)
        {
            RoleDao.Delete(new RoleInfo() { Id = id });

            return true;
        }

        public RoleInfo Get(int id)
        {
            return RoleDao.Find(id);
        }

        public List<RoleInfo> List()
        {
            return RoleDao.List();
        }

        public List<RoleInfo> GetByUser(int userId)
        {
            return RoleDao.GetByUser(userId);
        }

    }
    */
}
