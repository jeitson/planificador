using System.Collections.Generic;

//using reingenia.planificador.data.access;
//using reingenia.planificador.entity;
//using reingenia.planificador.@interface;

namespace reingenia.planificador.BusinessLogic
{
    /*
    public class Group : IGroup
    {

        internal GroupDao groupDao = null;

        internal GroupDao GroupDao
        {
            get
            {
                if (groupDao == null)
                    groupDao = new GroupDao();
                
                return groupDao;
            }
        }

        public GroupInfo Create(GroupInfo item)
        {
            return GroupDao.Create(item);
        }

        public GroupInfo Update(GroupInfo item)
        {
            return GroupDao.Update(item);
        }

        public bool Delete(int id)
        {
            GroupDao.Delete(new GroupInfo() { Id = id });

            return true;
        }

        public GroupInfo Get(int id)
        {
            return GroupDao.Find(id);
        }

        public List<GroupInfo> List()
        {
            return GroupDao.List();
        }

    }
    */
}
