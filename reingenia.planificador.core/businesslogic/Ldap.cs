using System.Collections.Generic;

//using reingenia.planificador.data.access;
//using reingenia.planificador.entity;
//using reingenia.planificador.@interface;

namespace reingenia.planificador.BusinessLogic
{
    /*
    public class Ldap : ILdap
    {

        internal LdapDao ldapDao = null;

        internal LdapDao LdapDao
        {
            get
            {
                if (ldapDao == null)
                    ldapDao = new LdapDao();
                
                return ldapDao;
            }
        }

        public LdapInfo Create(LdapInfo item)
        {
            return LdapDao.Create(item);
        }

        public LdapInfo Update(LdapInfo item)
        {
            return LdapDao.Update(item);
        }

        public bool Delete(int id)
        {
            LdapDao.Delete(new LdapInfo() { Id = id });

            return true;
        }

        public LdapInfo Get(int id)
        {
            return LdapDao.Find(id);
        }

        public List<LdapInfo> List()
        {
            return LdapDao.List();
        }

    }
    */
}
