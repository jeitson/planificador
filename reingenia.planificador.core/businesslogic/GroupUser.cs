using System.Collections.Generic;

//using reingenia.planificador.data.access;
//using reingenia.planificador.entity;
//using reingenia.planificador.@interface;

namespace reingenia.planificador.BusinessLogic
{
    /*
    public class GroupUser : IGroupUser
    {

        internal GroupUserDao groupuserDao = null;

        internal GroupUserDao GroupUserDao
        {
            get
            {
                if (groupuserDao == null)
                    groupuserDao = new GroupUserDao();
                
                return groupuserDao;
            }
        }

        public GroupUserInfo Create(GroupUserInfo item)
        {
            return GroupUserDao.Create(item);
        }

        public GroupUserInfo Update(GroupUserInfo item)
        {
            return GroupUserDao.Update(item);
        }

        public bool Delete(int groupId, int userId)
        {
            GroupUserDao.Delete(new GroupUserInfo() { GroupId = groupId, UserId = userId });

            return true;
        }

        public GroupUserInfo Get(int id)
        {
            return GroupUserDao.Find(id);
        }

        public List<GroupUserInfo> List()
        {
            return GroupUserDao.List();
        }

    }
    */
}
