using System.Collections.Generic;

//using reingenia.planificador.data.access;
//using reingenia.planificador.entity;
//using reingenia.planificador.@interface;

namespace reingenia.planificador.BusinessLogic
{
    /*
    public class RoleAccess : IRoleAccess
    {

        internal RoleAccessDao roleaccessDao = null;

        internal RoleAccessDao RoleAccessDao
        {
            get
            {
                if (roleaccessDao == null)
                    roleaccessDao = new RoleAccessDao();
                
                return roleaccessDao;
            }
        }

        public RoleAccessInfo Create(RoleAccessInfo item)
        {
            return RoleAccessDao.Create(item);
        }

        public RoleAccessInfo Update(RoleAccessInfo item)
        {
            return RoleAccessDao.Update(item);
        }

        public bool Delete(int id)
        {
            RoleAccessDao.Delete(new RoleAccessInfo() { Id = id });

            return true;
        }

        public RoleAccessInfo Get(int id)
        {
            return RoleAccessDao.Find(id);
        }

        public List<RoleAccessInfo> List()
        {
            return RoleAccessDao.List();
        }

        public List<RoleAccessInfo> ListByUser()
        {
            return RoleAccessDao.List();
        }

    }
    */
}
